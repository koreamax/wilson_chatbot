chromadb>=1.0.0
grpcio>=1.81.1
httpx>=0.27.0
pydantic-settings>=2.0.0
protobuf>=6.33.5
# dense 임베딩(ruri-v3-310m) 로컬 셀프호스팅용. rag.md 규칙: 외부 임베딩 API 금지.
# torch는 sentence-transformers의 의존성으로 딸려온다. GPU/CPU 휠 선택은
# 이미지 빌드(인프라) 영역 — 로컬 dev는 CPU 휠로 충분하다.
sentence-transformers>=3.0.0
