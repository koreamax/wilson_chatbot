apiVersion: v1
kind: Service
metadata:
  name: llm-server
spec:
  selector:
    app.kubernetes.io/name: llm-server
  ports:
    - name: grpc
      port: 50053
      targetPort: grpc
      appProtocol: grpc
